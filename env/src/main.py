from phi.agent import Agent 
from phi.environment import Environment
from phi.agent import Agent




essay_writting_agent = Agent(
    # Add functions or Toolkits
    tools=[],
    # Show tool calls in the Agent response
    show_tool_calls=True
)
